---
import { LoginForm } from "../components/auth/LoginForm";
import MainLayout from "../layouts/MainLayout.astro";

// Force server-side rendering to properly handle requests and headers
export const prerender = false;
---

<MainLayout>
  <LoginForm />
</MainLayout>
